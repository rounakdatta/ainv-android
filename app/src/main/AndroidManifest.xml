<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asslpl.ainv">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/asslpl_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".sales_deep_search"></activity>
        <activity android:name=".Register" />
        <activity android:name=".CustomerEntry" />
        <activity android:name=".ClientEntry" />
        <activity android:name=".view_sales" />
        <activity android:name=".searchSales" />
        <activity
            android:name=".Transaction"
            android:label="@string/title_activity_transaction"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity android:name=".ItemMasterEntry" />
        <activity android:name=".WarehouseEntry" />
        <activity android:name=".ActivityChooser" />
        <activity android:name=".view_inventory" />
        <activity android:name=".search" />
        <activity android:name=".user_onboarding" />
        <activity android:name=".start_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>